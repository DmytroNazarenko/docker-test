name: Release
on:
  workflow_run:
    workflows: ["Build and publish"]
    types: 
      - completed

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: |
        echo TEST
        
