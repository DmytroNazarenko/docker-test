name: Test
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: |
        cd ..;
    - uses: actions/checkout@v2
      with:
        repository: twbs/bootstrap
        ref: cursor-util
    - run: |
        cd ..; ls

    