name: Test
on: [push, pull_request]
jobs:
  test:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - run: |
        set /a %RANDOM% > end.txt
        ls
        cat end.txt

    