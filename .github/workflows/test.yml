name: Test
on: [push, pull_request]

jobs:
  browser-test:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - name: Docker run
      run: |
        docker run -d trufflesuite/ganache-cli:v6.8.1-beta.0
    - run: |
        docker ps