name: Test
on: [push, pull_request]
jobs:
  test:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - run: |
        $env:VAR_NAME="VALUE"
        $env:VAR_NAME > a.txt
        ls
        cat a.txt
    