name: Test
on: [push, pull_request]
jobs:
  test:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - run: |
        set /a (%RANDOM%*10000/32768) + 1000
        ls
        cat rnd.txt

    