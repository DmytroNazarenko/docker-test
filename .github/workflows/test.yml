name: Test
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: |
        ls; cd ..; ls
    - uses: actions/checkout@v2
      with:
        repository: twbs/bootstrap
        ref: cursor-util
        path: "../"
    - run: |
        ls; cd ..; ls

    