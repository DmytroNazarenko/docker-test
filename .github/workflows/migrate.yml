on:
  issues:
    types: [opened, edited]
    
    
jobs:
  migrate-issue:
    runs-on: ubuntu-latest
    steps:
    - name: Issue Forms Body Parser
      id: parse
      uses: zentered/issue-forms-body-parser@v2.0.0
      
    - name: Show parsed payload data
      run: |
        a=${{ github.event.issue.body }}
        echo "AAAA: $a";
